trigger:
  branches:
    include: 
    - develop
    - master
  paths:
    include: 
     - /src/*

resources:
  repositories:
  - repository: devops
    type: git
    name: pub/devops
    ref: master


jobs:
- template: npm-build-deploy.yml@devops
  parameters:
    packageName: 'svg-geo-map'
    hasUnitTests: true
    createPackage: true
    createGithubRelease: true